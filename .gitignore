# Do not track the OS download directory because it obviously
# tends to be a heavy one
setup/os/archives/*

## Rust configuration
# will have compiled files and executables
ss/ss-client/target/

# Remove Cargo.lock from gitignore if creating an executable, leave it for libraries
# More information here http://doc.crates.io/guide.html#cargotoml-vs-cargolock
ss/ss-client/Cargo.lock

# These are backup files generated by rustfmt
ss/ss-client/**/*.rs.bk

# Let's ignore the storage facility of the ss-client
ss/ss-client/ss-client_storage/

# Let's ignonre the log files as well.
ss/ss-client/log/*.log

# Ignore the web viz package files.
ss/ss-server/dist/*
ss/ss-server/node_modules/*

# Ignore node package lockfile.
ss/ss-server/package-lock.json

# Ignore the AWS IoT certificates.
ss/ss-client/certificates/*.pem*
ss/ss-server/certificates/*.pem*
